name: dzshoping
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.8.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  shared_preferences: ^2.3.2       # مكتبة التخزين المحلي
  image_picker: ^0.8.7+4          # مكتبة اختيار الصور من المعرض أو الكاميرا

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # أصول المشروع - إضافة مجلد الصور
  assets:
    - assets/images/
